import { AbstractEvent } from 'midi-writer-js/browser/types/abstract-event';
interface MidiEvent extends AbstractEvent {
    channel: number;
    status: number;
    buildData?: Function;
}
export { MidiEvent };
